# 프로젝트: 오픈소스 데이터셋 시각화·큐레이션 툴 (가칭)

이 문서는 프로젝트 초기 설계 논의를 정리한 것입니다. Claude Code가 매 세션 시작 시
이 파일을 읽고 아래 맥락을 기억한 상태로 작업을 시작할 수 있도록 작성했습니다.
결정이 바뀌거나 새로운 사실이 확인되면 이 파일도 함께 업데이트해주세요.

## 배경 / 문제의식

- FiftyOne(Voxel51)의 오픈소스/엔터프라이즈 기능 분리 방식이 마음에 들지 않아서 시작된 프로젝트.
  Enterprise가 파는 건 거의 대부분 "협업·거버넌스·스케일 인프라"(RBAC/SSO, 데이터셋 버전관리,
  cloud-backed media, 오케스트레이터)이고 핵심 기능(시각화·큐레이션·평가·임베딩) 자체는
  OSS에도 이미 다 있음.
- 이 업계 전체가 같은 사업모델로 수렴해 있음: 개인/로컬 워크플로우는 완전 무료로 풀고,
  팀 협업 인프라만 유료화. FiftyOne, Rerun(Rerun Hub) 둘 다 이 패턴.
- LightlyStudio(Rust 기반, Apache-2.0, 큐레이션+라벨링+임베딩 통합)가 2026년에 나와서
  같은 문제의식을 어느 정도 해결했지만, 이것도 결국 유료 전환할 가능성이 높다고 판단.
- **이 프로젝트의 목표: 완전히 무료로, 오픈 커뮤니티의 힘으로 지속되는 툴을 만든다.**
  수익모델을 코어 기능 유료화가 아니라 매니지드 호스팅/컨설팅 등으로만 잡거나,
  재단 형태 거버넌스를 고려.

## 전략: 자체 개발 최소화, 기존 OSS 조합

모든 걸 새로 만들지 않고 성숙한 오픈소스를 조합하는 것이 핵심 전략.
**진짜로 새로 만들어야 하는 건 "버전관리/협업 레이어 + 그 위의 대시보드 UX"뿐** —
나머지 레이어(로깅/시각화, 임베딩, 스토리지 등)는 기존 프로젝트를 최대한 활용.

## 핵심 요구사항 (우선순위 순으로 언급됨)

1. **효율성(성능)이 최우선** — 특히 대량 이미지/비디오 탐색 시 체감 속도
2. **대시보드를 통한 쉬운 사용성** — Python 몰라도 탐색·필터링·태깅·리뷰 가능해야 함
   (FiftyOne의 실제 약점 중 하나가 Python-first UX라 비개발자가 못 쓴다는 점)
3. **비디오·이미지·연속 시퀀스(멀티 카메라/센서 동기화) 처리**
4. **배포 환경(온프레미스/클라우드)에 구애받지 않는 쉬운 배포**
5. **실행취소(undo)가 쉽고, 작업이 제대로 됐는지 직관적으로 확인 가능해야 함**
6. **여러 사용자가 동시에 사용 가능하도록 세션이 사용자별로 분리**되어야 함
7. **세분화된 권한관리(ACL)·SSO는 코어에 무겁게 넣지 않고 opt-in으로 확장 가능하게**
   (그 정도 필요한 조직은 이미 그럴 역량이 있다는 판단)

## 경쟁 구도 요약

| | 라이선스 | 협업/권한관리 | 인프라 | 포커스 |
|---|---|---|---|---|
| FiftyOne OSS | Apache-2.0 | 없음 | MongoDB 필수 | 시각화·평가·임베딩 큐레이션 |
| FiftyOne Enterprise | 상용 | RBAC·SSO·서비스계정 | 자체 호스팅 | 거버넌스·오케스트레이션·billion-scale |
| LightlyStudio | Apache-2.0 (무료) | 코어 통합, 클라우드 협업판 예정 | Rust, MongoDB 불필요 | 큐레이션+라벨링+임베딩 통합 |
| Rerun SDK | MIT/Apache-2.0 | 없음 (Rerun Hub가 유료로 담당) | Rust | 멀티모달 시계열/로보틱스 시각화 |

## 설계 원칙 (논의 중 도출된 것들)

### 버전관리를 1급 시민으로
모든 mutation(태그, 라벨 수정 등)이 커밋 단위로 기록되는 git-like 데이터 모델.
- 임의 시점 조회, diff, 감사 로그가 자연스럽게 파생됨
- Undo = "직전 커밋으로 되돌리기" (별도 undo 스택 불필요)
- 배치 작업(수백 개 일괄 태그)도 커밋 하나로 묶으면 undo 한 번에 전체 롤백 가능
- **재고 후 대안**: 처음부터 다 직접 짜기보다 Apache Iceberg / Delta Lake의
  time-travel·스냅샷 기능을 메타데이터 레이어로 활용하고, 그 위에 샘플/라벨 단위
  diff UI만 얹는 방식이 자체 개발 범위를 줄여줄 수 있음 (DuckDB가 Iceberg/Delta 네이티브 지원)

### 인가(authorization)는 단일 인터페이스로 좁히기 (Policy Enforcement Point 패턴)
- 코드 전체에서 권한 체크를 `can(user, action, resource)` 같은 단일 함수로만 수행
- 기본 구현(코어, 항상 포함): 단순 role 체크
- 확장 구현(opt-in): 같은 인터페이스를 Casbin/OPA/Cerbos 등으로 교체 — 세밀한 ACL이
  필요한 조직은 자체 정책 붙이면 됨, 코어는 복잡한 ACL 로직을 몰라도 됨
- FastAPI를 쓴다면 `Depends()`가 이 문지기 함수를 넣기에 자연스러운 자리이고,
  Casbin의 fastapi-authz/fastapi-casbin-auth 같은 기존 어댑터를 바로 활용 가능
- 표준 업계 관행: XACML 참조모델의 PDP/PEP 분리, Google Zanzibar, OPA, Pundit/CanCanCan 등이
  전부 같은 패턴. OWASP Top 10에서 "Broken Access Control"이 꾸준히 상위권인 이유가
  바로 이 분리를 안 지켜서 생기는 문제라 오히려 이 패턴을 지키는 게 표준에 가까움
- SSO는 앱에 직접 구현하지 않고 oauth2-proxy/Authelia 같은 인증 프록시에 위임 —
  앱은 이미 검증된 유저 정보만 헤더/JWT로 읽음

### 세션 분리 ≠ 무거운 RBAC
- **공유 상태**: 실제 데이터셋(태그, 라벨, 커밋 히스토리) — 버전관리 레이어가 담당
- **세션 상태(유저별, ephemeral)**: 현재 필터, 뷰, 선택 상태, 미커밋 편집 — 세션 토큰에
  가볍게 붙여서 유저별로 완전 독립적으로 유지
- 동시 편집 충돌은 커밋 로그 순서 기반 낙관적 동시성 제어로 처리 (git처럼), 복잡한
  머지 UI는 필요 없을 가능성 높음
- Presence(누가 뭘 보는 중인지)는 웹소켓 기반으로 표시

### 비디오/연속 시퀀스
- 프레임을 미리 다 추출하지 말고 원본 압축 비디오 그대로 두고 온디맨드 디코딩/시킹
- 대시보드 프리뷰용 저해상도 프록시 비디오 별도 생성, 원본은 학습/추론 파이프라인용으로 보존
- 에피소드/시퀀스를 1급 단위로 모델링 (멀티 카메라/센서 동기화된 공유 타임라인) —
  이 문제는 Rerun SDK가 이미 잘 풀어놓음, 여기서 자체 개발 비중을 낮추는 게 이득
- 그리드 뷰에서 비디오는 필름스트립 프리뷰, 임베딩은 전체 디코딩 대신 키프레임 샘플링 기반
- 리뷰 단위는 CVAT처럼 청크 분할

### 배포 유연성
- 무거운 스테이트풀 외부 의존성(MongoDB 등) 지양 — 소규모는 DuckDB/SQLite, 확장 시 Postgres
- 오브젝트 스토리지는 S3 호환 API로 추상화 (온프렘 MinIO ↔ 클라우드 S3/GCS 엔드포인트만 교체)
  — 직접 클라이언트 짜기보다 Apache OpenDAL 같은 기존 추상화 라이브러리 활용 고려
- 컨테이너화 + 두 단계 난이도: 개인/소규모는 `docker compose up`, 팀/대규모는 같은 이미지를 Helm으로
- 아웃바운드 네트워크 필수 호출 없음 (에어갭 지원) — 텔레메트리/라이선스 체크는 옵트인
- 무거운 연산(임베딩, 트랜스코딩)은 API 서버와 분리된 워커 프로세스로 (큐 기반)

## 기술 스택 (초안 → 재검토 후 v2)

첫 논의에서는 FastAPI(Python) + React + DuckDB/Postgres + LanceDB + Rerun SDK + PyAV +
Casbin/OPA + oauth2-proxy + Docker Compose/Helm 조합으로 정리했음.

백지에서 다시 검토하며 나온 대안 (아직 최종 결정 아님, 검토 필요):

- **API 서버 언어: Go로 결정 (2026-08-07)**. 혼자 개발 + Claude Code 활용, 보여줄 대상
  없이 진행하는 상황이라 처음부터 최종 목표 언어로 맞추기로 함. Rust는 채택하지 않음 —
  지금 단계의 진짜 불확실성은 성능이 아니라 데이터 모델/버전관리/UX 구조인데, Rust의
  borrow checker가 이 구조를 계속 바꿔보며 탐색하는 초기 단계의 마찰을 크게 늘리기
  때문. Go는 단일 바이너리·쉬운 배포 목표를 그대로 충족하면서 문법이 단순해
  Claude Code 생성 코드 검토도 더 쉬움. 상시구동 서버(라우팅/세션/인가/커밋로그)는
  원래 설계상 I/O 바운드 CRUD 계층이라 Go로 충분하고, 무거운 연산(임베딩, 비디오
  트랜스코딩)은 계획대로 별도 워커 프로세스로 분리 — 워커는 나중에 필요시 Rust나
  Python으로 핫패스만 골라 교체 가능하므로 지금 Go를 선택해도 리라이트 리스크 없음
- **버전관리**: 처음부터 커스텀 구현 대신 Apache Iceberg/Delta Lake의 time-travel 위에
  얹고, 샘플/라벨 단위 diff UI만 자체 개발
- **실시간 협업/undo**: 손수 만든 커밋+웹소켓 대신 CRDT 라이브러리(Yjs)를 실시간 편집
  상태(태그/코멘트 등)에 활용 — undo·충돌 해결을 라이브러리가 대신 처리.
  단, ElectricSQL/Zero/LiveStore 같은 상위 "풀스택 로컬퍼스트 싱크 엔진"은 2026년 기준
  아직 실무 안정성 관련 상반된 평가가 있어 신중히 접근 — Yjs 정도의 검증된 하위
  라이브러리만 가져다 쓰는 게 지금은 더 안전한 선택
- **스토리지 추상화**: 직접 S3 클라이언트 대신 Apache OpenDAL
- **프론트엔드 렌더링**: React DOM 가상화로 시작하되, 실제 병목이 확인되면 Canvas/WebGPU
  기반 렌더링(Rerun 자체 웹뷰어 방식 참고)으로 교체 검토

## 아직 정해지지 않은 것 / 다음 단계

- 위 "v2 재검토" 항목들 중 API 서버 언어(Go) 외 나머지(버전관리 방식, 실시간 협업,
  스토리지 추상화, 프론트 렌더링)에 대한 최종 결정
- 프로젝트 이름, 리포지토리 구조
- MVP 스코프 정의 (전체를 한 번에 만들지 않는다면 어디서부터 시작할지)
- 거버넌스/라이선스 모델 구체화 (재단화 여부 등)

## 진행 상황 로그

- 2026-08-07: API 서버 언어를 Go로 확정. 이유는 위 "기술 스택" 섹션 참고.
- 2026-08-07: MVP v0 스캐폴딩 완료, 첫 git 커밋(`71ef225`). Go(stdlib net/http,
  modernc.org/sqlite) + React/Vite(react-window 가상화 그리드) + go:embed 단일 바이너리.
  로컬 폴더 인덱싱 → 썸네일 그리드 → 태그 커밋 → undo → 재시작 후 영속성까지 curl로
  end-to-end 검증 완료 (브라우저 자동화 도구가 없어 실제 화면 스크린샷 확인은 못 함 —
  다음 세션에서 `make build && ./data777 --data-dir ./devdata`로 직접 열어서 확인 필요).
  상세 설계는 `/home/jdj/.claude/plans/vast-meandering-boot.md` 참고.
  - 개발 환경: 이 WSL 머신엔 Go/Node가 기본 설치돼 있지 않았음. Go는 sudo 없이
    `$HOME/.local/go`에 공식 tarball로, Node는 nvm(`$HOME/.nvm`)으로 설치함. `~/.profile`에
    PATH가 추가돼 있어 새 로그인 세션에서는 자동으로 잡히지만, Claude Code의 Bash 도구는
    비대화형 쉘이라 `~/.bashrc`를 못 읽는 이슈가 있었음 — 이후 세션에서 `go`/`node`가
    안 잡히면 `export PATH="$HOME/.local/go/bin:$HOME/go/bin:$HOME/.nvm/versions/node/*/bin:$PATH"`
    를 먼저 실행할 것.