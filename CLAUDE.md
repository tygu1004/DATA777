# 프로젝트: 오픈소스 데이터셋 시각화·큐레이션 툴 (가칭)

이 문서는 프로젝트 초기 설계 논의를 정리한 것입니다. Claude Code가 매 세션 시작 시
이 파일을 읽고 아래 맥락을 기억한 상태로 작업을 시작할 수 있도록 작성했습니다.
결정이 바뀌거나 새로운 사실이 확인되면 이 파일도 함께 업데이트해주세요.

## 배경 / 문제의식

- FiftyOne(Voxel51)의 오픈소스/엔터프라이즈 기능 분리 방식이 마음에 들지 않아서 시작된 프로젝트.
  Enterprise가 파는 건 거의 대부분 "협업·거버넌스·스케일 인프라"(RBAC/SSO, 데이터셋 버전관리,
  cloud-backed media, 오케스트레이터)이고 핵심 기능(시각화·큐레이션·평가·임베딩) 자체는
  OSS에도 이미 다 있음.
- 이 업계 전체가 같은 사업모델로 수렴해 있음: 개인/로컬 워크플로우는 완전 무료로 풀고,
  팀 협업 인프라만 유료화. FiftyOne, Rerun(Rerun Hub) 둘 다 이 패턴.
- LightlyStudio(Rust 기반, Apache-2.0, 큐레이션+라벨링+임베딩 통합)가 2026년에 나와서
  같은 문제의식을 어느 정도 해결했지만, 이것도 결국 유료 전환할 가능성이 높다고 판단.
- **이 프로젝트의 목표: 완전히 무료로, 오픈 커뮤니티의 힘으로 지속되는 툴을 만든다.**
  수익모델을 코어 기능 유료화가 아니라 매니지드 호스팅/컨설팅 등으로만 잡거나,
  재단 형태 거버넌스를 고려.

## 전략: 자체 개발 최소화, 기존 OSS 조합

모든 걸 새로 만들지 않고 성숙한 오픈소스를 조합하는 것이 핵심 전략.
**진짜로 새로 만들어야 하는 건 "버전관리/협업 레이어 + 그 위의 대시보드 UX"뿐** —
나머지 레이어(로깅/시각화, 임베딩, 스토리지 등)는 기존 프로젝트를 최대한 활용.

**이 전략은 "의존성 최소화"가 아니다 (2026-08-10 명확화).** 사용자 의도는 오히려
반대에 가깝다 — 최근 널리 쓰이고 인기 있는 오픈소스를 기술 스택 전반에 적극 반영해
여러 분야 개발자가 매력을 느끼고 기여하고 싶어지는 프로젝트로 만드는 것. 새 의존성을
추가하지 않을 이유는 없고, 검증된 도구는 적극 채택한다. 기준은 "적게 쓴다"가 아니라
"직접 만들지 말고 좋은 걸 가져다 쓴다".

## 핵심 요구사항 (우선순위 순으로 언급됨)

1. **효율성(성능)이 최우선** — 특히 대량 이미지/비디오 탐색 시 체감 속도
2. **대시보드를 통한 쉬운 사용성** — Python 몰라도 탐색·필터링·태깅·리뷰 가능해야 함
   (FiftyOne의 실제 약점 중 하나가 Python-first UX라 비개발자가 못 쓴다는 점)
3. **비디오·이미지·연속 시퀀스(멀티 카메라/센서 동기화) 처리**
4. **배포 환경(온프레미스/클라우드)에 구애받지 않는 쉬운 배포**
5. **실행취소(undo)가 쉽고, 작업이 제대로 됐는지 직관적으로 확인 가능해야 함**
6. **여러 사용자가 동시에 사용 가능하도록 세션이 사용자별로 분리**되어야 함
7. **세분화된 권한관리(ACL)·SSO는 코어에 무겁게 넣지 않고 opt-in으로 확장 가능하게**
   (그 정도 필요한 조직은 이미 그럴 역량이 있다는 판단)

## 경쟁 구도 요약

| | 라이선스 | 협업/권한관리 | 인프라 | 포커스 |
|---|---|---|---|---|
| FiftyOne OSS | Apache-2.0 | 없음 | MongoDB 필수 | 시각화·평가·임베딩 큐레이션 |
| FiftyOne Enterprise | 상용 | RBAC·SSO·서비스계정 | 자체 호스팅 | 거버넌스·오케스트레이션·billion-scale |
| LightlyStudio | Apache-2.0 (무료) | 코어 통합, 클라우드 협업판 예정 | Rust, MongoDB 불필요 | 큐레이션+라벨링+임베딩 통합 |
| Rerun SDK | MIT/Apache-2.0 | 없음 (Rerun Hub가 유료로 담당) | Rust | 멀티모달 시계열/로보틱스 시각화 |

## 설계 원칙 (논의 중 도출된 것들)

### 버전관리를 1급 시민으로
모든 mutation(태그, 라벨 수정 등)이 커밋 단위로 기록되는 git-like 데이터 모델.
- 임의 시점 조회, diff, 감사 로그가 자연스럽게 파생됨
- Undo = "직전 커밋으로 되돌리기" (별도 undo 스택 불필요)
- 배치 작업(수백 개 일괄 태그)도 커밋 하나로 묶으면 undo 한 번에 전체 롤백 가능
- **재고 후 대안**: 처음부터 다 직접 짜기보다 Apache Iceberg / Delta Lake의
  time-travel·스냅샷 기능을 메타데이터 레이어로 활용하고, 그 위에 샘플/라벨 단위
  diff UI만 얹는 방식이 자체 개발 범위를 줄여줄 수 있음 (DuckDB가 Iceberg/Delta 네이티브 지원)

### 인가(authorization)는 단일 인터페이스로 좁히기 (Policy Enforcement Point 패턴)
- 코드 전체에서 권한 체크를 `can(user, action, resource)` 같은 단일 함수로만 수행
- 기본 구현(코어, 항상 포함): 단순 role 체크
- 확장 구현(opt-in): 같은 인터페이스를 Casbin/OPA/Cerbos 등으로 교체 — 세밀한 ACL이
  필요한 조직은 자체 정책 붙이면 됨, 코어는 복잡한 ACL 로직을 몰라도 됨
- FastAPI를 쓴다면 `Depends()`가 이 문지기 함수를 넣기에 자연스러운 자리이고,
  Casbin의 fastapi-authz/fastapi-casbin-auth 같은 기존 어댑터를 바로 활용 가능
- 표준 업계 관행: XACML 참조모델의 PDP/PEP 분리, Google Zanzibar, OPA, Pundit/CanCanCan 등이
  전부 같은 패턴. OWASP Top 10에서 "Broken Access Control"이 꾸준히 상위권인 이유가
  바로 이 분리를 안 지켜서 생기는 문제라 오히려 이 패턴을 지키는 게 표준에 가까움
- SSO는 앱에 직접 구현하지 않고 oauth2-proxy/Authelia 같은 인증 프록시에 위임 —
  앱은 이미 검증된 유저 정보만 헤더/JWT로 읽음

### 세션 분리 ≠ 무거운 RBAC
- **공유 상태**: 실제 데이터셋(태그, 라벨, 커밋 히스토리) — 버전관리 레이어가 담당
- **세션 상태(유저별, ephemeral)**: 현재 필터, 뷰, 선택 상태, 미커밋 편집 — 세션 토큰에
  가볍게 붙여서 유저별로 완전 독립적으로 유지
- 동시 편집 충돌은 커밋 로그 순서 기반 낙관적 동시성 제어로 처리 (git처럼), 복잡한
  머지 UI는 필요 없을 가능성 높음
- Presence(누가 뭘 보는 중인지)는 웹소켓 기반으로 표시

### 비디오/연속 시퀀스
- 프레임을 미리 다 추출하지 말고 원본 압축 비디오 그대로 두고 온디맨드 디코딩/시킹
- 대시보드 프리뷰용 저해상도 프록시 비디오 별도 생성, 원본은 학습/추론 파이프라인용으로 보존
- 에피소드/시퀀스를 1급 단위로 모델링 (멀티 카메라/센서 동기화된 공유 타임라인) —
  이 문제는 Rerun SDK가 이미 잘 풀어놓음, 여기서 자체 개발 비중을 낮추는 게 이득
- 그리드 뷰에서 비디오는 필름스트립 프리뷰, 임베딩은 전체 디코딩 대신 키프레임 샘플링 기반
- 리뷰 단위는 CVAT처럼 청크 분할

### 배포 유연성
- **필수** 외부 의존성(FiftyOne의 MongoDB처럼 없으면 아예 안 도는 것)은 지양하되,
  **선택적** 외부 엔진은 적극 채택 (2026-08-10 의도 명확화). 사용자가 원래 생각한 건
  FiftyOne이 Milvus를 별도 컨테이너로 띄워 연결하듯 "필요할 때 붙이는 플러그인"이었음 —
  이건 이 프로젝트가 이미 쓰는 패턴(`storage.Source` 뒤의 Local/S3, `can()` 뒤의 Casbin/OPA)과
  동일하다. 구분해야 할 건 "외부냐 내장이냐"가 아니라 "필수냐 선택이냐".
  소규모는 SQLite로 외부 서비스 0개, 1천만 장 초과 시 ClickHouse 연결. 상세는 `docs/architecture.md`
- 오브젝트 스토리지는 S3 호환 API로 추상화 (온프렘 RustFS ↔ 클라우드 S3/GCS 엔드포인트만 교체)
  — 구현 완료, `aws-sdk-go-v2` 채택. OpenDAL은 검토 후 탈락(아래 "기술 스택" 참고)
- 컨테이너화 + 두 단계 난이도: 개인/소규모는 `docker compose up`, 팀/대규모는 같은 이미지를 Helm으로
- 아웃바운드 네트워크 필수 호출 없음 (에어갭 지원) — 텔레메트리/라이선스 체크는 옵트인
- 무거운 연산(임베딩, 트랜스코딩)은 API 서버와 분리된 워커 프로세스로 (큐 기반)

## 기술 스택 (초안 → 재검토 후 v2)

첫 논의에서는 FastAPI(Python) + React + DuckDB/Postgres + LanceDB + Rerun SDK + PyAV +
Casbin/OPA + oauth2-proxy + Docker Compose/Helm 조합으로 정리했음.

백지에서 다시 검토하며 나온 대안 (아직 최종 결정 아님, 검토 필요):

- **API 서버 언어: Go로 결정 (2026-08-07)**. 혼자 개발 + Claude Code 활용, 보여줄 대상
  없이 진행하는 상황이라 처음부터 최종 목표 언어로 맞추기로 함. Rust는 채택하지 않음 —
  지금 단계의 진짜 불확실성은 성능이 아니라 데이터 모델/버전관리/UX 구조인데, Rust의
  borrow checker가 이 구조를 계속 바꿔보며 탐색하는 초기 단계의 마찰을 크게 늘리기
  때문. Go는 단일 바이너리·쉬운 배포 목표를 그대로 충족하면서 문법이 단순해
  Claude Code 생성 코드 검토도 더 쉬움. 상시구동 서버(라우팅/세션/인가/커밋로그)는
  원래 설계상 I/O 바운드 CRUD 계층이라 Go로 충분하고, 무거운 연산(임베딩, 비디오
  트랜스코딩)은 계획대로 별도 워커 프로세스로 분리 — 워커는 나중에 필요시 Rust나
  Python으로 핫패스만 골라 교체 가능하므로 지금 Go를 선택해도 리라이트 리스크 없음
- **버전관리**: 커밋 = (태그, op, roaring 비트맵)으로 확정 (2026-08-10). 샘플당 행을
  쌓지 않으므로 5억 장 일괄 태그도 수 MB고 undo는 비트맵 연산 한 번. 이걸 자체 커밋
  로그로 둘지 Iceberg 스냅샷에 얹을지는 아직 미결이나, `Catalog` 인터페이스 뒤라
  나중에 바꿔도 앱 코드가 안 바뀌므로 지금 정하지 않아도 됨
- **실시간 협업/undo**: 손수 만든 커밋+웹소켓 대신 CRDT 라이브러리(Yjs)를 실시간 편집
  상태(태그/코멘트 등)에 활용 — undo·충돌 해결을 라이브러리가 대신 처리.
  단, ElectricSQL/Zero/LiveStore 같은 상위 "풀스택 로컬퍼스트 싱크 엔진"은 2026년 기준
  아직 실무 안정성 관련 상반된 평가가 있어 신중히 접근 — Yjs 정도의 검증된 하위
  라이브러리만 가져다 쓰는 게 지금은 더 안전한 선택
- **스토리지 추상화: `aws-sdk-go-v2` S3 클라이언트로 결정 (2026-08-07)**. 처음엔 Apache
  OpenDAL을 후보로 뒀으나, Go 바인딩이 CGO는 안 쓰더라도 `opendal-c`라는 미리 컴파일된
  네이티브 공유 라이브러리(purego+libffi로 런타임 동적 로드)에 의존한다는 걸 확인 —
  플랫폼별 네이티브 바이너리를 따로 배포해야 해서 "단일 정적 바이너리, `docker compose up`,
  에어갭 지원"이라는 Go 채택 이유와 정면충돌. 실제 요구사항은 "S3 호환 API로 자체호스팅/S3/GCS
  엔드포인트만 바꿔 쓰기"뿐이라, 순수 Go인 `aws-sdk-go-v2`의 S3 클라이언트로 커스텀
  엔드포인트/path-style만 설정하면 충분하다고 판단. `internal/storage` 패키지에 `Source`
  인터페이스(`Walk`, `Open`)를 두고 `Local`/`S3` 두 구현체로 분리, indexer와 thumbnail
  생성기가 이 인터페이스만 바라보도록 리팩터링 완료.
- **자체호스팅 S3 서버 예시: MinIO 대신 RustFS로 결정 (2026-08-07)**. 라이선스 문제 —
  MinIO 서버는 AGPLv3라 네트워크로 서비스 제공 시 소스 공개 의무가 걸리고, 최근 OSS판에서
  웹 콘솔(Console)까지 유료판으로 옮겨감 — 이 프로젝트가 처음부터 피하려던 FiftyOne류
  "핵심 기능 유료화" 패턴과 같음. RustFS(Apache-2.0, Rust)는 MinIO와의 마이그레이션/공존을
  염두에 두고 만들어진 S3 호환 오브젝트 스토리지로, 웹 콘솔이 기본 포함. 다만 2026-08-07
  기준 버전이 아직 `1.0.0-beta.x`라 프로덕션 안정성은 미검증 — path-style 주소지정
  명시 문서화는 못 찾음(MinIO와 같은 관행일 것으로 추정, 실제 배포 전 검증 필요).
  코드 쪽은 이미 프로토콜 제네릭이라 변경 불필요, `internal/storage/s3.go`·`main.go`의
  예시 주석/플래그 설명만 RustFS 언급으로 갱신.
- **프론트엔드 렌더링: Pixi v8 WebGPU로 확정 (2026-08-10)**. 원래는 "DOM 가상화로 시작,
  병목 확인되면 Canvas/WebGPU 검토"였으나, 목표 규모가 10억 장으로 확정되면서 그리드는
  결국 GPU로 갈 수밖에 없다고 판단 — DOM 그리드를 정성껏 스타일링한 뒤 버리는 것보다
  한 번에 가는 게 낫다. Pixi v8은 WebGPU 우선 + WebGL 자동 폴백이라 미지원 환경도 커버.
  `react-window`는 제거하고, 윈도잉 계산은 렌더러 비종속인 TanStack Virtual이 담당
  (캔버스에도 그대로 쓸 수 있음). 나머지 프론트 스택은 React 19 + Tailwind v4 +
  shadcn/ui + TanStack Query. 상세는 `docs/architecture.md`

## 아직 정해지지 않은 것 / 다음 단계

2026-08-10에 목표 규모(10M~1B)와 데이터 계층·프론트 렌더링이 확정됐다.
**확정 내용의 정본은 `docs/architecture.md`와 `docs/api.md`이며(둘 다 커밋됨),
이 파일은 세션 맥락용 요약과 결정 로그만 유지한다.** 남은 미결:

- **버전관리 구현 방식** — 커밋 = (태그, op, roaring 비트맵)까지는 확정. 다만 이걸
  자체 커밋 로그로 갈지 Iceberg 스냅샷에 얹을지는 미결(Iceberg는 `Catalog` 인터페이스
  뒤에 나중에 끼워도 앱 코드가 안 바뀌므로 지금 결정 안 해도 됨)
- **실시간 협업** — Yjs(CRDT) 도입 여부. 세션 분리는 확정이나 presence/동시편집은 미착수
- 프로젝트 이름, 리포지토리 구조
- 거버넌스/라이선스 모델 구체화 (재단화 여부 등)
- ClickHouse 구현체, 아틀라스 엔드포인트, 필터 사이드바 UI — 계약만 정의됨, 구현 대기
- **구조적 미비는 `docs/roadmap.md`에 정리됨.** 1~7번(2026-08-10 도출, 유사 프로젝트
  FiftyOne·LightlyStudio·Rerun·CVAT·lakeFS 비교)은 전부 계약 차원에서 해결. 2026-08-11에
  계약끼리 서로 검토하며 8~10번 추가 — **8번(크로스 엔진 필터)·9번(데이터셋 스코프)은
  아직 열려 있음**, 10번(미디어/시퀀스 모델)은 `docs/media.md`로 해결. 유일하게 계약을 **수정**해야 했던 3번(데이터 모델)은 같은 날 처리 완료 —
  `docs/api.md`에 타입 있는 필드 스키마, `Filter`의 술어 트리 일반화, `set`/`patch`
  두 종류 커밋 모델 반영함. 나머지 6개는 기존 계약을 깨지 않고 확장하는 항목이라 순서
  유연 — 2번(확장 지점)도 같은 세션에 처리 완료(`docs/plugins.md`). 이어서 6번(작업
  모델)도 예정보다 앞당겨 처리 완료 — 2번의 긴 Operator와 3번의 `set` 커밋 둘 다 여기
  의존하고 있었음. 4번(벡터 인덱스)도 같은 세션에 이어서 처리 완료. 1번(Python SDK)도
  이어서 처리 완료. 다음 세션(2026-08-11)에 남았던 5번(뷰 파이프라인)·7번(버전관리
  경계 명시)도 처리 완료 — 7개 전부 계약 차원에서는 해결됐다.
  **다만 이건 문서(계약) 작업일 뿐, `internal/`·`web/`의 실제 코드는 여전히 2026-08-07의
  tags-only MVP 그대로다** (동기 커밋, job 없음, 타입 필드 없음, 프론트는 아직
  `react-window` + React 18). 다음 단계는 이 v2 계약들을 실제로 구현하는 것이고,
  이건 이 로그 위쪽의 "기술 스택" 결정들처럼 머신 분리를 지켜야 하는 규모의 작업이다
  (백엔드는 데스크톱, 프론트는 맥북에어) — `docs/roadmap.md`의 "Suggested order" 하단에
  같은 내용을 기록해둠.

## 개발 환경 (머신별 역할 분리)

랩실 데스크톱 / 집 데스크톱 / 맥북에어 세 대를 오가며 개발한다.

- **데스크톱(WSL)**: 백엔드(Go), 스토리지/엔진 작업
- **맥북에어**: WSL이 없으므로 프론트엔드·UI 담당
- 맥북에서 `node`/`npm`은 nvm(`~/.nvm`, v24.19.0)으로 설치돼 있는데 비대화형 셸에서는
  PATH에 안 잡힘 — `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"`를 먼저 실행할 것.
  Go(1.26.5)는 그냥 잡힘.

## 진행 상황 로그

- 2026-08-07: API 서버 언어를 Go로 확정. 이유는 위 "기술 스택" 섹션 참고.
- 2026-08-07: MVP v0 스캐폴딩 완료, 첫 git 커밋(`71ef225`). Go(stdlib net/http,
  modernc.org/sqlite) + React/Vite(react-window 가상화 그리드) + go:embed 단일 바이너리.
  로컬 폴더 인덱싱 → 썸네일 그리드 → 태그 커밋 → undo → 재시작 후 영속성까지 curl로
  end-to-end 검증 완료 (브라우저 자동화 도구가 없어 실제 화면 스크린샷 확인은 못 함 —
  다음 세션에서 `make build && ./data777 --data-dir ./devdata`로 직접 열어서 확인 필요).
  (상세 설계 노트는 당시 세션의 로컬 플랜 파일에 있었고 리포에는 없음 — 확정 내용은
  이후 `docs/`로 옮겨졌으므로 그쪽을 정본으로 볼 것.)
- 2026-08-07: 사용자 실사용 피드백으로 버그 두 개 수정.
  1. undo해도 커밋 히스토리가 안 바뀌어 보이던 문제 — `ListCommits`이 전체 커밋을 나열하던
     걸 HEAD에서 parent_id를 따라가는 체인만 보여주도록 변경. shift+클릭 범위선택,
     전체선택 버튼도 함께 추가 (`5d37312`).
  2. 확대 프리뷰(라이트박스, 줌 포함) 추가 — `yet-another-react-lightbox` +
     `react-virtualized-auto-sizer` 사용, 직접 구현하지 않음 (`5ead2ef`).
  3. **undo를 커밋이 0개가 될 때까지 누르면 화면이 통째로 사라지는 크래시** 발견·수정
     (`7f007e6`). 원인: Go `encoding/json`이 빈 슬라이스를 `null`로 직렬화하는데
     프론트가 `commits.find()`를 그대로 호출해 렌더 중 예외 발생 → ErrorBoundary가
     없어서 전체 트리가 unmount됨. 백엔드는 빈 리스트를 항상 `[]`로 반환하도록 고치고,
     프론트에도 방어 코드 + 루트 ErrorBoundary 추가.
  - **브라우저 자동화 확보**: Playwright(Python, `pip3 install --user`)로 헤드리스
    Chromium 설치 완료 — 위 3번 버그를 이걸로 실제 재현·검증함. 프로젝트 의존성에는
    안 넣었고 테스트 전용. 앞으로 프론트 변경은 curl만으로 끝내지 말고 이걸로
    실제 화면을 찍어서 확인할 것.
- 2026-08-07: v2 재검토 중 스토리지 추상화 항목 결정 및 구현 완료. 결정 이유는 위
  "기술 스택" 섹션 참고. `go get aws-sdk-go-v2`(config/s3) 추가, `internal/storage`
  패키지 신설(`Source` 인터페이스 + `Local`/`S3` 구현), indexer/thumbnail이 `os.Open`/
  `filepath.WalkDir` 직접 호출 대신 이 인터페이스를 쓰도록 리팩터링, `main.go`에
  `--storage local|s3`, `--s3-bucket`, `--s3-region`, `--s3-endpoint`, `--s3-path-style`
  플래그 추가 (S3 자격증명은 AWS SDK 표준 체인 사용, 별도 플래그 없음). 빌드 확인 +
  local 백엔드로 Playwright 회귀 테스트(그리드/태그/커밋/undo) 재실행해 기존 동작 그대로
  유지됨을 확인. S3 경로는 아직 실제 RustFS/S3에 붙여서 검증하지 않음 — 다음에 필요하면
  로컬 RustFS 컨테이너로 확인할 것.
- 2026-08-07: 지난 세션에서 못했던 실제 화면 검증을 Playwright 스크린샷으로 완료.
  `make build` → `./data777 --data-dir ./devdata --port 8777` 로 띄운 뒤 그리드 로드,
  더블클릭 라이트박스(줌 포함), 썸네일 선택 + shift 범위선택, 태그 추가 → 커밋 생성,
  undo를 커밋이 0개가 될 때까지 반복 — 전 과정에서 콘솔 에러·페이지 에러·크래시 없음
  확인 (`7f007e6` 수정이 실제로 먹힘). 백엔드 로그도 에러 없이 깨끗함. 스크린샷은
  세션 스크래치패드에만 저장, 리포에는 커밋 안 함.
- 2026-08-10 (맥북에어 첫 세션): **목표 규모를 10M~1B로 확정하고 그에 맞춰 아키텍처 전면
  재설계.** "UI를 FiftyOne 급으로"에서 출발했으나, 현재 구조가 대규모에서 구조적으로
  깨진다는 게 드러나 아키텍처 결정 작업으로 전환했다.
  - **발견된 문제**: `useSamples`가 전체 데이터셋을 메모리로 로드한다(첫 페이지 후 나머지
    전 페이지를 `Promise.all`로 동시 요청해 누적). 여기 딸린 가정들 — `App.tsx`의
    `sampleIds` 전체 배열, `selectAll`의 전체 ID `Set`, 태그 커밋의 샘플당 op 1개,
    `ListSamples`의 `LIMIT/OFFSET`, 페이지마다 `COUNT(*)`, `commit_ops`의 (샘플,태그)당
    행 1개 — 이 7가지가 전부 10억 장에서 무너진다. UI만 먼저 다듬으면 이 위에 쌓게 되어
    나중에 전부 갈아엎어야 했음.
  - **확정**: 3층 데이터 구조(샘플 메타데이터 SQLite→ClickHouse / 커밋·HEAD·세션
    SQLite→Postgres / 태그는 roaring 비트맵 BLOB), Go 유지 + 외부 엔진 위임,
    계약은 "값의 나열이 아니라 디스크립터"(Filter·Selection). Postgres 전환 트리거는
    다중 사용자가 아니라 **다중 서버**임을 명확히 함 — 사용자 10명이어도 쓰는 건 Go
    프로세스 하나라 SQLite로 충분하고, API 서버를 여러 대로 늘릴 때 필요해짐.
  - **조사 결과(실제 확인)**: `iceberg-go`·`clickhouse-go`·`RoaringBitmap/roaring`은 순수 Go.
    `go-duckdb`는 CGO 필수. **LanceDB Go SDK는 CGO + 사전 빌드 네이티브 라이브러리 별도
    다운로드** 방식이라 OpenDAL을 탈락시킨 것과 같은 이유로 탈락(Rust였다면 1순위였을 것).
    Lance가 아쉬워서 Go 고수 여부를 재검토했으나, 엔진을 외부에 두는 구조에서는 Rust 전환의
    실익이 없어 Go 유지로 결론.
  - **프론트 스택 확정**: React 19(+`@pixi/react` v8 peer 요구), Tailwind v4 + shadcn/ui,
    TanStack Query(단 `useInfiniteQuery` 금지 — 청크 sparse query), TanStack Virtual,
    Pixi v8 WebGPU 그리드(`react-window` 제거). 기존 의존성 3개 모두 React 19 지원 확인함.
  - **산출물**: `docs/architecture.md`, `docs/api.md` 신규 작성(둘 다 커밋 대상). 코드 변경
    없음 — 구현은 다음 세션부터. `docs/`가 정본이고 이 파일은 요약·로그만 유지.
- 2026-08-10 (같은 세션, 계속): CLAUDE.md 추적 시작(개인 경로 정리 후 커밋), 유사 프로젝트
  (FiftyOne·LightlyStudio·Rerun·CVAT·lakeFS/DVC) 비교로 구조적 미비 7개 도출해
  `docs/roadmap.md` 신규 작성. 그중 데이터 모델(3번)을 같은 세션에 처리:
  - **필드 스키마 신설**: `scalar`(고정 코어 필드) / `tags`(비트맵) / `labels`(데이터셋별
    선언, `classification`·`detection`·`keypoints` 타입) 세 종류. `GET /api/schema`,
    `POST /api/schema/fields` 추가.
  - **`Filter` 일반화**: 태그·width 같은 하드코딩된 최상위 키를 없애고 `match` 술어 배열로
    통일 (`{field, op, value}`). `labels` 필드는 `elem_match`로 라벨 객체 하나에 여러 조건이
    동시에 걸리는 걸 표현(예: "신뢰도 0.5 미만인 car 탐지").
  - **커밋을 `set`/`patch` 두 종류로 분리** — 이게 이번 세션에서 가장 중요한 발견. 비트맵
    기반 `set`은 "추가의 역은 제거"라는 성질 덕에 공짜로 undo되는데, 이건 **집합 소속에만
    성립**하고 스칼라 값에는 성립 안 함(값을 되돌리려면 이전 값을 알아야 함). 그래서 바운딩
    박스 좌표 수정 같은 라벨 값 편집은 `patch`(샘플별 값, 서버가 이전 값 저장)로 별도 분리.
    `patch`가 대규모에서 안전한 이유는 사람이 리뷰 세션당 수십~수천 개를 편집하기 때문이지
    데이터셋 크기와 무관해서임 — 애초에 10억 개를 손으로 편집할 일이 없음.
  - **의도적으로 안 풀고 남긴 문제**: 파이프라인이 대규모 선택 범위에 스칼라 필드를 일괄
    덮어쓰는 경우의 저렴한 undo. `set`의 공짜 역연산이 집합 소속에만 통하므로, 이 경우는
    `O(affected)` 비용을 받아들이거나 아직 없는 압축 기법이 필요 — `docs/api.md`·
    `docs/roadmap.md`에 명시적으로 미해결로 남겨둠(조용히 얼버무리지 않음).
  - `docs/architecture.md`의 3층 데이터 구조 표에도 라벨 상태 행 추가(샘플 메타데이터와
    같은 SQLite에 두되, 변형 경로가 태그·메타데이터 어느 쪽과도 다르다는 점 명시).
- 2026-08-10 (같은 세션, 계속): roadmap 2번(확장 지점) 처리, `docs/plugins.md` 신규 작성.
  - **Operator/Panel 둘로 분리**(FiftyOne 용어 그대로 채택 — 이 프로젝트가 끌어들이려는
    바로 그 사용자층에게 이미 익숙한 이름). Go는 인프로세스 플러그인이 사실상 안 되므로
    (`plugin` 패키지가 리눅스 전용에 취약) 플러그인은 그냥 HTTP 서비스 — 이미 있는
    "선택적 외부 부착물" 패턴을 애플리케이션 레이어에 그대로 적용한 것.
  - **핵심 통찰**: Operator는 특별한 쓰기 경로가 필요 없다. 셀렉션을 읽고 `set`/`patch`
    커밋을 쓰는 걸 공개 API로 하면 되므로, **Operator는 사실상 "선언된 폼 + 대시보드가
    실행 버튼을 눌러주는 SDK 스크립트"**에 불과함. 이게 곧 1번(SDK)과 2번(확장 지점)이
    같은 메커니즘을 공유한다는 뜻이고, 별도 쓰기 프로토콜을 설계할 필요가 없어짐.
  - **보안·배포 판단**: 동적 자체 등록 대신 정적 config 파일로만 플러그인 등록(플러그인은
    커밋을 쓸 수 있는 신뢰를 받으므로 프로세스 스스로 부여하게 두지 않음). Operator 실행과
    Panel UI 둘 다 **data777 서버가 프록시**하고 브라우저가 플러그인에 직접 접속하지
    않도록 함 — docker-compose에서 포트 하나만 열면 되고 CORS 설정이 아예 필요 없어짐
    (기본값이 아니라 의도적으로 고른 선택).
  - **의도적으로 남긴 연결고리**: 오래 걸리는 Operator(임베딩 계산 등)는 `job_id`를
    반환할 수 있게 계약을 열어뒀지만, 그 job API 자체는 아직 없음(6번) — 6번이 이제
    "추가하면 좋은 것"에서 "2번이 의존하는 것"으로 격상됨, roadmap에 반영.
- 2026-08-10 (같은 세션, 계속): 사용자가 "태깅이 대규모에선 배치 작업이 될 텐데 job으로
  던지고, 실패하면 되돌리고, 대기 중 취소도 되어야 하지 않냐"고 지적 — 정확한 지적이라
  roadmap 6번(작업 모델)을 원래 계획보다 앞당겨 이 자리에서 처리. `docs/api.md`에
  `## Jobs` 섹션 신설, `POST /api/commits`·`POST /api/undo`·`POST /api/index`를 전부
  `202` + `job_id`로 전환(기존 동기 `201` 응답 폐기).
  - **왜 동기 응답이 실제로 문제였는지**: `set` 커밋을 적용하려면 필터에 매칭되는
    샘플들의 비트맵을 처음 계산해야 하는데, 5억 장이 매칭되면 ClickHouse에서 그만큼의
    ID를 스트리밍해서 비트맵을 만드는 데 시간이 걸림(초~분 단위) — HTTP 요청 하나로
    묶어두기엔 부적합.
  - **롤백을 별도로 구현할 필요가 없다는 게 핵심 발견**: 이미 "비트맵을 완전히 계산한
    뒤 한 번에 원자적으로 커밋 로그에 붙이는" 설계였기 때문에, 계산 도중 실패해도 아직
    실제 상태(HEAD, 태그 비트맵)엔 아무것도 반영 안 됨. 실패=롤백이 아니라 애초에
    끝까지 안 갔으면 아무것도 안 바뀐 것 — 취소도 같은 이유로 언제 끊어도 안전.
  - 작은 커밋(샘플 몇 개 태깅)이 체감상 여전히 즉시 처리되게 `?wait=Ns` 롱폴링을 넣음 —
    클라이언트가 job 생성 직후 짧게 대기하면 대부분 그 안에 끝나서, 규모별로 API를
    나눌 필요가 없어짐.
  - 기존 `POST /api/index`·`GET /api/index/status`(전역 상태 하나짜리 임시방편)도 이
    job 모델로 흡수 — 취소 가능해지고 나머지와 동일한 형태가 됨.
  - Go 쪽 설계: `Catalog`의 `ApplySet`/`ApplyPatch`/`Undo`는 그대로 동기 Go 메서드로
    유지(프로세스 내부 호출은 오래 걸려도 문제 아님), 비동기 경계는 별도
    `internal/jobs` 패키지가 담당 — HTTP 핸들러가 워커 고루틴에서 이 메서드들을 호출하고
    `context.Context` 취소로 중단을 전파.
  - **의도적으로 미해결로 남긴 것**: 서버 재시작 시 진행 중이던 job의 처리 방식. job
    레코드 자체는 영속화해 이력은 남지만, Go 고루틴은 재시작을 못 버티므로 재시작 시점에
    `running`/`queued`였던 job은 "재시작으로 중단됨" 에러로 `failed` 처리하는 게 맞는
    동작이라고 `docs/roadmap.md`에 명시만 해두고 구현은 안 함.
  - `docs/plugins.md`의 Operator 실행 응답도 이 job 모델에 맞춰 통일(항상 `job_id`
    반환, 이전엔 빠른/느린 두 가지 응답 모양이 따로 있었음).
  - **부수 작업**: roadmap.md 제목을 "structural"에서 "resolved 2026-08-10"으로 바꾸며
    Markdown 앵커가 바뀌어 다른 문서의 링크 4개 중 2개가 깨졌던 것 발견·수정 — 문서
    상호 링크를 앵커로 걸 때 생기는 흔한 함정이라 남겨둠.
- 2026-08-10 (같은 세션, 계속): roadmap 4번(임베딩·유사도 검색) 처리.
  - **핵심 구분**: 임베딩은 태그·라벨과 달리 커밋 대상이 아님 — 사람이 내린 판단이
    아니라 모델이 계산한 값이라 undo할 "결정"이 없음. 그래서 4번째 필드 종류
    `embedding`을 신설하되, `set`/`patch` 커밋 경로가 아니라 별도 벌크 upsert
    엔드포인트(`POST /api/embeddings/{field}`)로 뺌 — 썸네일 생성과 같은 계열로 취급.
  - `Filter`에 `near` 연산자(벡터 또는 `sample_id` 참조 + `max_distance`), `sort`에
    `near` 변형을 추가해 "샘플 1042와 비슷한 것부터" 같은 쿼리를 기존 `/api/samples`
    페이지네이션 계약 그대로 처리 — 별도 검색 엔드포인트를 만들지 않음.
  - **의도적으로 그은 범위선**: 근접 중복 제거·다양성 샘플링·이상치 탐지는 `near`
    위에 얹는 큐레이션 워크플로우지 새 쿼리 문법이 아님 — Operator(2번)가 `near`로
    검색하고 `set` 커밋을 쓰는 형태로 구현되어야 한다고 명시. 필터 언어에 워크플로우별
    동사를 계속 붙이고 싶은 유혹을 피함.
  - **저장 엔진**: 다른 계층과 같은 소규모→대규모 전개 — 기본은 SQLite BLOB
    브루트포스(~100만 벡터까지 실용적, 정확한 검색), 그 이상은 외부 ANN 엔진. Qdrant를
    추천으로 명시(순수 Go 클라이언트, Apache-2.0, 필터링 검색 네이티브 지원) — 사용자가
    원래 염두에 뒀던 Milvus는 거부가 아니라 대안으로 남겨둠.
  - `VectorIndex`를 `Catalog`와 별도 Go 인터페이스로 분리(둘은 독립적으로 선택되는
    배포 결정이라). `Search`가 `Filter`를 같이 받을 때 필터링된 ANN 검색의 정확도
    트레이드오프(엔진마다 다름)를 얼버무리지 않고 명시.
  - roadmap 4번 → "resolved" 처리 후 문서 간 앵커 재검증(이번엔 깨진 것 없음 확인).
- 2026-08-10 (같은 세션, 계속): roadmap 1번(Python SDK) 처리. 사용자가 "맥북에서 할
  작업이 아니면 알려달라"고 확인했는데, 이번에도 순수 문서 작업이라 무관하다고 답한 뒤
  진행 — 실제 Go/React 코드 작성 단계에 들어가면 그때 머신 분리 적용해야 함을 재확인.
  - `docs/sdk.md` 신규 작성 — Pydantic 타입 클라이언트, 서버 로직 재구현 없이 기존
    엔드포인트를 그대로 감싸는 얇은 계층으로 설계.
  - SDK가 실제로 필요로 하는데 계약에 없던 3가지를 `docs/api.md`에 먼저 채워 넣음:
    1) **토큰 인증** (`POST/GET/DELETE /api/tokens`) — 세분화된 스코프는 만들지 않고
       기존 authz 철학(코어는 단순 역할 체크, 세밀한 ACL은 opt-in)을 그대로 재사용.
    2) **커서 기반 전체 순회** — `offset`은 그리드용(위치 점프), `cursor`는 스크립트의
       전체 순회용(키셋 페이지네이션). 후자가 필요한 이유는 O(offset) 비용 회피뿐
       아니라, 순회 도중 데이터가 바뀌어도 건너뛰거나 중복되지 않는다는 안정성 때문 —
       두 캐릭터가 다른 요구를 가지므로 하나로 통합하지 않고 둘 다 유지.
    3) **`at_commit` 커밋 고정 읽기** — 학습용 내보내기가 도중에 HEAD가 움직여도
       영향받지 않게 함. 과거 커밋 시점 상태 재구성 비용은 "샘플 수"가 아니라
       "그 시점과 현재 사이 커밋 개수"에 비례한다는 걸 명시(커밋은 큐레이션 행위
       단위라 수천~수백만 개 수준이지 샘플당 하나가 아님).
  - **내보내기(COCO/YOLO/Parquet)는 서버에 안 만들고 SDK 쪽 클라이언트 로직으로**
    — 4번(임베딩) 때 근접중복제거·다양성샘플링을 서버 문법이 아니라 Operator로
    돌린 것과 같은 종류의 판단. 포맷별 로직을 서버에 넣기 시작하면 ML 프레임워크가
    늘 때마다 서버 표면적이 늘어남.
  - roadmap 7개 중 5개(1,2,3,4,6) 처리 완료. 남은 5번(뷰 파이프라인)·7번(버전관리
    경계 명시)은 서로 독립적이고 이번 세션에서 다룬 것들보다 훨씬 가벼움.
- 2026-08-11 (데스크톱): 사용자 요청으로 **전체 설계·명세 검토**를 수행하고, 그 결과로
  미디어/시퀀스 모델(`docs/media.md`)을 신설. 검토는 "요구사항 충족" 기준이 아니라
  "FiftyOne/LightlyStudio에서 좋은 건 차용하고 나쁜 건 개선" 기준으로 진행 — 계약이 큰 건
  의도된 것이므로 문제로 보지 않기로 명확히 함.
  - **발견한 치명적 오류 2개 — 같은 날 병행 세션에서 수정 완료, 실측 검증까지 끝남**:
    1. **`set` 커밋의 "공짜 역연산"이 성립하지 않음.** `api.md`는 "add cat to S"의 역이
       "remove cat from S"라고 하는데, S 안에 이미 cat이 있던 샘플이 있으면
       `(B ∪ S) \ S = B \ S ≠ B` — undo가 원래 있던 태그까지 지움. 저장할 비트맵을
       *매칭 집합* S가 아니라 *실제 델타*(add면 `S \ B`, remove면 `S ∩ B`)로 바꾸면 해결.
       `affected_count` 의미도 "매칭 수"에서 "변경 수"로 바뀜(사용자에게 더 정확한 숫자).
       **이 오류가 다섯 세션의 문서 리뷰를 통과했다는 게 "구현으로 계약을 때려봐야 한다"의
       근거** — `Undo()` 10분만 짜봤으면 죽었을 버그.
    2. **단일 전역 HEAD + 다중 사용자 = 서로의 커밋을 지움.** `POST /api/undo`가 인자 없이
       HEAD를 부모로 옮기므로, A가 "내 작업 취소"를 누르면 그 사이 들어온 B의 커밋이
       사라짐. `expected_head` 받고 409를 내거나, undo를 git revert처럼 "되돌리는 새 커밋
       생성"으로 재정의하는 게 나음(후자면 redo·고아 커밋·감사 로그가 전부 같이 풀림).
  - 그 외 지적: 긴 job의 write-write 경합 미정의, 사용자/세션/커밋 작성자가 계약에 아예
    없음(감사 로그인데 누가 했는지 기록 없음), `plugins.md`의 "scoped token"이 `api.md`의
    "coarse-grained"와 모순, `near`+`count`/`near`+`cursor` 정의 불가, 임의 정렬에 대한
    offset이 한 문장에 서브시스템을 숨기고 있음, 썸네일 *생성* 비용이 각주 처리됨.
  - **roadmap 8~10번 신설**: 8번(필터가 조인 불가능한 엔진들을 가로지름 — ClickHouse의
    네이티브 roaring 비트맵 지원이 해법일 가능성), 9번(데이터셋 스코프가 없는데 문서는
    "per dataset"이라 씀), 10번(비디오/시퀀스 — 이번에 해결).
  - **`docs/media.md`의 핵심 결정**: **프레임을 서브도큐먼트가 아니라 샘플로** 둔다
    (FiftyOne과 정반대). 이 프로젝트의 모든 프리미티브가 이미 `sample_id`로 키잉돼 있어서
    태그 비트맵·라벨·임베딩·`near`·커밋·undo가 프레임에 코드 0줄로 적용됨. 추가된 건
    스칼라 5개(`media_type`, `parent_id`, `group_id`, `t`, `slice`) + 예약돼 있던 `group`
    스테이지를 `rollup`으로 구체화 + 플러그인 매니페스트의 `media_handlers` 하나뿐.
    `group_id`=타임라인의 정체성, `t`=그 위의 위치, `slice`=어느 센서, `parent_id`=어느
    미디어에서 파생 — 30fps 카메라와 10Hz 라이다를 프레임 번호로 못 맞추므로 Rerun에서
    "t는 공유 타임라인 위의 초"만 빌리고 다중 명명 타임라인은 안 가져옴.
    프레임 폭발은 인덱싱 시 stride(기본 1fps)로 막고, 조밀화는 오퍼레이터.
    필름스트립 = 아틀라스라 썸네일 생성 문제도 같이 해결(비디오당 순차 디코딩 1회).
  - 문서 상호 링크 앵커를 스크립트로 전수 검증(이전에 두 번 깨졌던 지점) — 전부 정상.
  - **같은 날 v2 구현이 병행 세션에서 완료됨** (백엔드 `internal/{catalog,jobs,auth,plugins,
    vectorindex}`, 프론트 Pixi 그리드, `sdk/`). 위 오류 2개는 그 과정에서 수정됐고,
    실제로 띄워서 검증 완료: 겹치는 태그 커밋(`affected_count`가 매칭 6이 아닌 델타 3으로
    보고) → undo → 원래 태그 보존 확인, `expected_head` 불일치 시 409, rollup·sample+seed
    (같은 seed면 같은 draw), 미디어 필드 전 계층 배선, Playwright로 UI 콘솔 에러 0.
  - **남은 구멍**: 테스트 파일이 0개 — 방금 손으로 검증한 델타/undo가 정확히 자동 검증이
    필요한 로직임. `plugins.md`의 "scoped token"과 `api.md`의 "coarse-grained" 모순 미해결.
    **UI가 백엔드보다 한참 뒤처져 있음** — 프론트 587줄로 v1 태그 워크플로우만 노출하고,
    라벨 오버레이·술어 빌더·유사도 검색·아틀라스가 전부 없음(다음 우선순위).